name: Run Shell Commands
on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a String
      run: echo "Hello, This is Engineer Moses!!! This is my first Github Actions Workflow file"